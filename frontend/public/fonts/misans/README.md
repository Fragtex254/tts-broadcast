# MiSans font assets

This directory contains the app-bundled MiSans font files used by the `modern` UI font preset.

Do not remove these files unless the font preset is changed at the product level. The frontend intentionally loads MiSans from `/fonts/misans/` so the interface does not depend on fonts installed on the user's computer.
